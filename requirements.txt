# ============================================================
# Ghost Creator AI — all dependencies (CLI + GUI + backends)
# ============================================================
# Install: pip install -r requirements.txt

# --- Core ---
python-dotenv>=1.0.0

# --- GUI / API ---
# customtkinter removed — GUI is Electron + React
markdown>=3.5
fastapi>=0.115.0
python-multipart>=0.0.18
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# --- Research ---
pytrends>=4.9.2
feedparser>=6.0.10
requests>=2.31.0

# --- Scripting (Gemini) ---
google-genai>=0.5.0

# --- Voice / TTS ---
# OmniVoice server runs in separate repo/environment (external run.bat path),
# so local omnivoice/torch stack is intentionally not required here.
pydub>=0.25.1
edge-tts>=6.1.9
elevenlabs>=1.0.0
# Spoken-word numbers for TTS (Hindi/Devanagari, Telugu, English, etc.)
num2words>=0.5.12
open-tamil>=1.1.0



# --- Image generation (Gemini Imagen; thumbnails) ---
Pillow>=10.3.0

# --- Video / numeric ---
numpy>=1.26.0

# --- YouTube uploader + Analytics ---
playwright>=1.43.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.100.0

# --- Documentary mode (footage download) ---
yt-dlp>=2024.1.1

# --- Utilities ---
colorlog>=6.8.2
